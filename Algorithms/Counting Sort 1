 static int[] countingSort(int[] arr) {
        int[] result= new int[100];
        Arrays.fill(result,0);
        for(int i=0;i<arr.length;i++){
            result[arr[i]]++;
        }
        return result;

    }
