static String catAndMouse(int x, int y, int z) {
        if(Math.abs(z-y)==Math.abs(z-x)){
            return "Mouse C";
        }
        if(Math.abs(z-y)>Math.abs(z-x)){
            return "Cat A";
        }
        else
        return "Cat B";
    }
