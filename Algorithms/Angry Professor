static String angryProfessor(int k, int[] a) {
        int student=0;
        for(int i=0;i<a.length;i++){
            if(a[i]<=0){
                student++;
            }
        }
        if(student>=k) return "NO";
        else return "YES";

    }
