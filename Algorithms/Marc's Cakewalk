 static long marcsCakewalk(int[] calorie) {
        Arrays.sort(calorie);
        long miles=0;
        int power=0;
        for(int i=calorie.length-1;i>=0;i--){
            miles+=(Math.pow(2,power))*calorie[i];
            power++;
        }
    return miles;
    }
