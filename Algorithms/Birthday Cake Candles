 static int birthdayCakeCandles(int[] ar) {
        int count=1;
        Arrays.sort(ar);
        for(int i=0;i<ar.length-1;i++){
            if(ar[ar.length-1]==ar[i]){
                count++;
            }
        }
        return count;
    }
